# Local speech recognition, translation and voice. Apple Silicon (MLX) only.
# yt-dlp[default] brings yt-dlp-ejs, and deno is the JavaScript runtime it needs
# to read YouTube's stream links; without it downloads fail with HTTP 403.
yt-dlp[default]
deno
mlx-whisper
mlx-lm
piper-tts
# Spells out numbers for letter-based voices (the Ukrainian one).
num2words
